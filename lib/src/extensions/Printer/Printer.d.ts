import { GeneralOptions } from '../../type';
import { Extension } from '@tiptap/core';

export interface PrinterOptions extends GeneralOptions<PrinterOptions> {
}
export declare const Printer: Extension<PrinterOptions, any>;
